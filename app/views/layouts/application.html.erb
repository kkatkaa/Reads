<!DOCTYPE html>
<html>
  <head>
    <title>Reads</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if notice %>
      <div class="alert alert-success">
        <%= notice %>
      </div>
    <% end %>

    <% if alert %>
      <div class="alert alert-danger">
        <%= alert %>
      </div>
    <% end %>

    <div class="container-1">
      <%= render 'layouts/navbar' %>
    </div>

    <div class="container-2"> <i>“A reader lives a thousand lives before he dies . . . The man who never reads lives only one.”</i> – <b>George R.R. Martin</b></div>
      <hr />
    </div>

    <div class="container-3">
      <%= yield %>
    </div>
  </body>
</html>
